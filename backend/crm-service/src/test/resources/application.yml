spring:
  datasource:
    url: jdbc:h2:mem:crm_test_db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false
  rabbitmq:
    listener:
      simple:
        auto-startup: false
      direct:
        auto-startup: false

cloudinary:
  cloud-name: demo
  api-key: demo_key
  api-secret: demo_secret

app:
  jwt:
    secret: 9a4f2c8d7e6b5a3f1c2d3e4f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d
  gateway:
    secret: eventos_gateway_secure_shared_secret

service:
  auth:
    base-url: http://localhost:8081/api/v1/auth
  event:
    base-url: http://localhost:8083/api/v1/events
